{{yield
  (hash
    selected=selected
    options=options
    isEmpty=isEmpty
    toggleAction=(action "toggle")
    selectAllAction=(action "selectAll")
    selectNoneAction=(action "selectNone")
)}}
